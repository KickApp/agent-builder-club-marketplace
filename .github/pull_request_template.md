This repository is a one-way deploy target from the private `KickApp/agent-factory` repo.

Skill and plugin work belongs in `abc/` there (staged in `abc/marketplace/`, then deployed).
Pull requests opened here for skill changes will be closed.
